channel
{{ @model.name }}
<ul>
{{#each @model.messages as |message| }}
	<Message @message={{message}} />
{{/each}}

</ul>
<ChatInput />
<BsButton @type="primary" {{on "click" this.insertMessage }}>Test</BsButton>

{{outlet}}